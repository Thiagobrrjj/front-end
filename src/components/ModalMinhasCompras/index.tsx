import React from "react";

const ModalMinhasCompras = ({ onClose }: any) => {
  return (
    <div className="fixed inset-0 ">
      <div className="bg-white w-full h-full">batata</div>
    </div>
  );
};

export default ModalMinhasCompras;
